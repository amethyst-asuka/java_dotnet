Namespace org.omg.PortableServer


	''' <summary>
	''' org/omg/PortableServer/POAManagerOperations.java .
	''' Generated by the IDL-to-Java compiler (portable), version "3.2"
	''' from d:/re/puppet/workspace/8-2-build-windows-amd64-cygwin/jdk8u73/6086/corba/src/share/classes/org/omg/PortableServer/poa.idl
	''' Friday, January 29, 2016 5:40:18 PM PST
	''' </summary>


	''' <summary>
	''' Each POA object has an associated POAManager object. 
	''' A POA manager may be associated with one or more 
	''' POA objects. A POA manager encapsulates the processing 
	''' state of the POAs it is associated with.
	''' </summary>
	Public Interface POAManagerOperations

	  ''' <summary>
	  ''' This operation changes the state of the POA manager 
	  ''' to active, causing associated POAs to start processing
	  ''' requests. </summary>
	  ''' <exception cref="AdapterInactive"> is raised if the operation is
	  '''            invoked on the POAManager in inactive state. </exception>
	  Sub activate()

	  ''' <summary>
	  ''' This operation changes the state of the POA manager 
	  ''' to holding, causing associated POAs to queue incoming
	  ''' requests. </summary>
	  ''' <param name="wait_for_completion"> if FALSE, the operation 
	  '''            returns immediately after changing state.  
	  '''            If TRUE, it waits for all active requests 
	  '''            to complete. </param>
	  ''' <exception cref="AdapterInactive"> is raised if the operation is
	  '''            invoked on the POAManager in inactive state. </exception>
	  Sub hold_requests(ByVal wait_for_completion As Boolean)

	  ''' <summary>
	  ''' This operation changes the state of the POA manager 
	  ''' to discarding. This causes associated POAs to discard
	  ''' incoming requests. </summary>
	  ''' <param name="wait_for_completion"> if FALSE, the operation 
	  '''            returns immediately after changing state.  
	  '''            If TRUE, it waits for all active requests 
	  '''            to complete. </param>
	  ''' <exception cref="AdapterInactive"> is raised if the operation is
	  '''            invoked on the POAManager in inactive state. </exception>
	  Sub discard_requests(ByVal wait_for_completion As Boolean)

	  ''' <summary>
	  ''' This operation changes the state of the POA manager 
	  ''' to inactive, causing associated POAs to reject the
	  ''' requests that have not begun executing as well as
	  ''' as any new requests. </summary>
	  ''' <param name="etherealize_objects"> a flag to indicate whether
	  '''        to invoke the etherealize operation of the
	  '''        associated servant manager for all active 
	  '''        objects. </param>
	  ''' <param name="wait_for_completion"> if FALSE, the operation 
	  '''            returns immediately after changing state.  
	  '''            If TRUE, it waits for all active requests 
	  '''            to complete. </param>
	  ''' <exception cref="AdapterInactive"> is raised if the operation is
	  '''            invoked on the POAManager in inactive state. </exception>
	  Sub deactivate(ByVal etherealize_objects As Boolean, ByVal wait_for_completion As Boolean)

	  ''' <summary>
	  ''' This operation returns the state of the POA manager.
	  ''' </summary>
	  Function get_state() As org.omg.PortableServer.POAManagerPackage.State
	End Interface ' interface POAManagerOperations

End Namespace